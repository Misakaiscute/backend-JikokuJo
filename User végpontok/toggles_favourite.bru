meta {
  name: toggles_favourite
  type: http
  seq: 5
}

post {
  url: {{api_url}}/routes/favourite/toggle
  body: json
  auth: bearer
}

auth:bearer {
  token: {{api_token}}
}

body:json {
  {
    "trip_id": "C0418318C"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
