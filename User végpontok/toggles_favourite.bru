meta {
  name: toggles_favourite
  type: http
  seq: 5
}

post {
  url: {{api_url}}/routes/favourite/toggle
  body: json
  auth: bearer
}

auth:bearer {
  token: {{api_token}}
}

body:json {
  {
    "route_id": "0070",
    "time": "1230"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
