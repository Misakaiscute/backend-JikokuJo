meta {
  name: delete_user
  type: http
  seq: 6
}

delete {
  url: {{api_url}}/user/delete
  body: none
  auth: bearer
}

auth:bearer {
  token: {{api_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
